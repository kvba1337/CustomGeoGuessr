import React from "react";
import "./NextRoundNoGoogling.scss";

const NoGoogling = () => (
  <div className="no-googling">REMEMBER: GOOGLING IS NOT ALLOWED</div>
);

export default React.memo(NoGoogling);
